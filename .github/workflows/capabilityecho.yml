name: CapabilityEcho

on:
  pull_request:

permissions:
  contents: read

jobs:
  capability-drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: ./
        with:
          fail-on: none
